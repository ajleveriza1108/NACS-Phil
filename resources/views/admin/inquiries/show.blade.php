@extends('admin.layouts.app', ['title' => 'Inquiry'])
@section('content')
<div><a href="{{ route('admin.inquiries.index') }}" class="text-sm font-bold text-nacs-700">← Back to inquiries</a><h1 class="section-title mt-3 text-4xl">Inquiry from {{ $inquiry->guardian_name }}</h1><p class="mt-2 text-slate-500">Received {{ $inquiry->created_at->format('F j, Y g:i A') }}</p></div>
<div class="mt-7 grid gap-6 xl:grid-cols-[1fr_420px]">
<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><dl class="grid gap-5 sm:grid-cols-2"><div><dt class="text-xs font-bold uppercase tracking-wider text-slate-500">Parent/guardian</dt><dd class="mt-1 font-semibold">{{ $inquiry->guardian_name }}</dd></div><div><dt class="text-xs font-bold uppercase tracking-wider text-slate-500">Student</dt><dd class="mt-1 font-semibold">{{ $inquiry->student_name ?: 'Not supplied' }}</dd></div><div><dt class="text-xs font-bold uppercase tracking-wider text-slate-500">Email</dt><dd class="mt-1">{{ $inquiry->email ?: 'Not supplied' }}</dd></div><div><dt class="text-xs font-bold uppercase tracking-wider text-slate-500">Phone</dt><dd class="mt-1">{{ $inquiry->phone ?: 'Not supplied' }}</dd></div><div><dt class="text-xs font-bold uppercase tracking-wider text-slate-500">Level interested</dt><dd class="mt-1">{{ $inquiry->level_interested }}</dd></div><div><dt class="text-xs font-bold uppercase tracking-wider text-slate-500">Privacy consent</dt><dd class="mt-1">Recorded {{ $inquiry->privacy_consent_at->format('M j, Y g:i A') }}</dd></div></dl><div class="mt-7"><h2 class="font-bold">Message</h2><div class="mt-3 whitespace-pre-line rounded-xl bg-slate-50 p-5 leading-7">{{ $inquiry->message }}</div></div></section>
<form method="POST" action="{{ route('admin.inquiries.update', $inquiry) }}" class="h-fit space-y-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">@csrf @method('PATCH')<h2 class="section-title text-2xl">Staff follow-up</h2><label class="block"><span class="font-bold">Status</span><select name="status" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3">@foreach(['new'=>'New','contacted'=>'Contacted','awaiting_response'=>'Awaiting response','closed'=>'Closed'] as $value=>$label)<option value="{{ $value }}" @selected(old('status', $inquiry->status) === $value)>{{ $label }}</option>@endforeach</select></label><label class="block"><span class="font-bold">Private staff notes</span><textarea name="admin_notes" rows="10" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3">{{ old('admin_notes', $inquiry->admin_notes) }}</textarea><small class="mt-1 block text-slate-500">Visible only to authorized administrators. Do not store unnecessary sensitive data.</small></label><button class="w-full rounded-xl bg-nacs-700 px-5 py-3 font-bold text-white">Save follow-up</button></form>
</div>
@endsection
