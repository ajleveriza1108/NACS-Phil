@extends('admin.layouts.app', ['title'=>'Admissions Applications'])
@section('content')
<section class="cm-page-head"><div><a class="cm-back-link" href="{{ route('admin.dashboard') }}">&larr; Content Manager</a><span class="cm-eyebrow">Private records</span><h1>Admissions Applications</h1><p>Principal and Super Admin review only. Keep applicant information private.</p></div></section>
<form method="GET" class="p9c-filters"><input name="search" value="{{ $search }}" placeholder="Reference, guardian, student..."><select name="status"><option value="">All statuses</option>@foreach($statuses as $value=>$label)<option value="{{ $value }}" @selected($status===$value)>{{ $label }}</option>@endforeach</select><button class="cm-button cm-button--secondary">Filter</button></form>
<section class="cm-panel cm-panel--wide"><div class="p9c-app-list">@forelse($applications as $application)<a href="{{ route('admin.admissions.show',$application) }}"><div><strong>{{ $application->student_name }}</strong><small>{{ $application->guardian_name }} &middot; {{ $application->reference_code }}</small></div><div><span class="p9-role-chip">{{ $application->statusLabel() }}</span><small>{{ $application->applying_for_level }} &middot; {{ $application->documents_count }} document(s)</small></div></a>@empty<div class="cm-empty">No admissions applications yet.</div>@endforelse</div><div class="p9c-pagination">{{ $applications->links() }}</div></section>
@endsection