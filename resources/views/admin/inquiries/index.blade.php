@extends('admin.layouts.app', ['title' => 'Inquiries'])
@section('content')
<div><p class="eyebrow">Admissions contact</p><h1 class="section-title mt-2 text-4xl">Inquiries</h1><p class="mt-2 text-slate-600">Basic contact inquiries only. Do not request report cards, medical records, or other sensitive documents here.</p></div>
<form method="GET" class="mt-6 flex flex-wrap gap-3"><select name="status" class="rounded-xl border border-slate-300 px-4 py-3"><option value="">All statuses</option>@foreach(['new'=>'New','contacted'=>'Contacted','awaiting_response'=>'Awaiting response','closed'=>'Closed'] as $value=>$label)<option value="{{ $value }}" @selected($status === $value)>{{ $label }}</option>@endforeach</select><button class="rounded-xl bg-nacs-700 px-5 py-3 font-bold text-white">Filter</button></form>
<div class="mt-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-4">Parent/guardian</th><th class="px-5 py-4">Level</th><th class="px-5 py-4">Status</th><th class="px-5 py-4">Received</th><th class="px-5 py-4"></th></tr></thead><tbody class="divide-y divide-slate-100">
@forelse($inquiries as $inquiry)<tr><td class="px-5 py-4"><strong>{{ $inquiry->guardian_name }}</strong><span class="block text-slate-500">{{ $inquiry->email ?: $inquiry->phone ?: 'No contact supplied' }}</span></td><td class="px-5 py-4">{{ $inquiry->level_interested }}</td><td class="px-5 py-4 capitalize">{{ str_replace('_', ' ', $inquiry->status) }}</td><td class="px-5 py-4 text-slate-500">{{ $inquiry->created_at->format('M j, Y g:i A') }}</td><td class="px-5 py-4 text-right"><a href="{{ route('admin.inquiries.show', $inquiry) }}" class="rounded-lg border border-slate-300 px-3 py-2 font-bold">Open</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No inquiries found.</td></tr>@endforelse
</tbody></table></div><div class="border-t border-slate-200 px-5 py-4">{{ $inquiries->links() }}</div></div>
@endsection
