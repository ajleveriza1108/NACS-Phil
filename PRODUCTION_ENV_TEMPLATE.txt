# NACS-Phil production environment template
# Copy these VALUES manually into the real server's .env.
# NEVER commit the real .env or replace placeholders in this tracked template.

APP_NAME="NACS-Phil"
APP_ENV=production
APP_KEY=GENERATE_ON_SERVER_WITH_PHP_ARTISAN_KEY_GENERATE
APP_DEBUG=false
APP_URL=https://YOUR-FINAL-DOMAIN

APP_LOCALE=en
APP_FALLBACK_LOCALE=en

APP_MAINTENANCE_DRIVER=file
BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# Recommended shared-hosting production database.
DB_CONNECTION=mysql
DB_HOST=YOUR_DATABASE_HOST
DB_PORT=3306
DB_DATABASE=YOUR_DATABASE_NAME
DB_USERNAME=YOUR_DATABASE_USER
DB_PASSWORD=YOUR_DATABASE_PASSWORD

# Keep sessions server-side and secure them for HTTPS.
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax

# These database-backed defaults match the current project.
CACHE_STORE=database
QUEUE_CONNECTION=database

# Private files remain on Laravel's local/private disk.
# Public Gallery/media files use the explicit "public" disk in code.
FILESYSTEM_DISK=local

BROADCAST_CONNECTION=log

# Configure a real SMTP provider only when outgoing email is enabled.
MAIL_MAILER=smtp
MAIL_SCHEME=tls
MAIL_HOST=YOUR_SMTP_HOST
MAIL_PORT=587
MAIL_USERNAME=YOUR_SMTP_USERNAME
MAIL_PASSWORD=YOUR_SMTP_PASSWORD
MAIL_FROM_ADDRESS=YOUR_OFFICIAL_SCHOOL_EMAIL
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

# Adaptive anti-bot protection for public forms and Admin sign-in.
# Create a Managed Turnstile widget for the final hostname before launch.
TURNSTILE_ENABLED=true
TURNSTILE_SITE_KEY=YOUR_CLOUDFLARE_TURNSTILE_SITE_KEY
TURNSTILE_SECRET_KEY=YOUR_CLOUDFLARE_TURNSTILE_SECRET_KEY
TURNSTILE_EXPECTED_HOSTNAME=YOUR-FINAL-DOMAIN
